<!-- Navigation-->
<nav class="navbar navbar-expand-lg navbar-light fixed-top shadow-sm flex" id="mainNav">
    <div class="containerNav px-5">
        <a class="navbar-brand fw-bold" href="{{path('actualites')}}">
            <img src="/img/logo.jpg" class="navbar-brand-img" alt="logo" width="150px">
        </a>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ms-auto me-4 my-3 my-lg-0">
                <li class="nav-item"><a class="nav-link me-lg-3" href="{{path('actualites')}}">Les patisseries du moment</a></li>
                <li class="nav-item"><a class="nav-link me-lg-3" href="{{path('aPropos')}}">Qui sommes nous</a></li>
                <li class="nav-item"><a class="nav-link me-lg-3" href="{{path('boutiques')}}">Nos boutiques</a></li>
                <li class="nav-item"><a class="nav-link me-lg-3" href="{{path('contact')}}">Nous contacter</a></li>
            </ul>
        </div>
    </div>
    <div class="pink">
        <ul class="">
        {% for categorie in categorieNavbar() %}
            <li>
                <a  class="" href="{{ path('portfolio_categorie', {'slug' : categorie.slug}) }}">{{ categorie.nom }}</a>
            </li>
        {% endfor %}
        </ul>
    </div>
</nav>